<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="tech.jianshuo.fiji.biz.persistence.mapper.RoleMapper">

    <select id="findRolesByUserId" parameterType="Long">
        SELECT r.id, r.name, r.description
        FROM fj_role r
        INNER JOIN fj_user_role ur ON ur.role_id = r.id
        WHERE ur.user_id = #{userId}
    </select>

</mapper>